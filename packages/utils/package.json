{
  "name": "@jumpwind/utils",
  "version": "0.0.1",
  "type": "module",
  "license": "MIT",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./src/*": "./src/*.ts",
    "./internal/*": null,
    "./package.json": "./package.json"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "format": "biome check",
    "test": "bun run vitest run",
    "test:watch": "bun run vitest --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "effect": "catalog:effect"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:frontend",
    "typescript": "catalog:",
    "vitest": "catalog:frontend"
  }
}
